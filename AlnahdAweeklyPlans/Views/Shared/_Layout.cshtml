<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="~/Content/Site.css" rel="stylesheet" type="text/css" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />

    <link href="~/Content/bootstrap-theme.css" rel="stylesheet" />
    <link href="~/Content/font-awesome.css" rel="stylesheet" />
    <link href="~/Content/bootstrap-datepicker.css" rel="stylesheet"/>
    <link href="~/Content/chosen.css" rel="stylesheet" />
    <link href="~/Content/Site.css" rel="stylesheet" />
    
    <script src="~/Scripts/modernizr-2.6.2.js"></script>
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @Html.ActionLink("Application name", "Index", "WeeklyPlan", new { area = "" }, new { @class = "navbar-brand" })
            </div>
            <div class="navbar-collapse collapse">
                <ul class="nav navbar-nav"></ul>
            </div>
        </div>
    </div>
    <script src="~/Scripts/jquery-3.0.0.js"></script>
    <div class="container body-content">
        @RenderBody()
    </div>
 
    <script src="~/Scripts/bootstrap.js"></script>
    <script src="~/Scripts/chosen.jquery.js"></script>
    <script src="~/Scripts/ckeditor/ckeditor.js"></script>
    <script src="~/Scripts/bootstrap-datepicker.js"></script>
    <script type="text/javascript">
        $("#pickdate").click(function () {
            $("#pickdate").datepicker("show");
        });
        $(".noticeboarddate").click(function () {
            $(this).datepicker("show");
        });
        $(document).on("changeDate", "#pickdate", function (e) {
            var date = $(this).data("datepicker").getDate();
          //  var day = $(this).data("datepicker").format("dddd");
            formatted = date.getFullYear() + "-" + (date.getMonth() + 1) + "-" + date.getDate();
            // $('#weekDay').val(date.format('dddd'));
            var weekday = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
            var selectedday = new Date(date).getDay();
            var selecteddate = new Date(date).getDate();
          //  var selecteddate = new Date(date);
            //var day = weekday[date];      
            //alert(date);
            var mindate = null;
            var maxdate = null;
            var inc = 0;
            for (var i = selectedday; i < 4; i++) {
                inc = inc + 1;
            }
            maxdate = selecteddate + inc;
            var enddate = date.setDate(date.getDate() + inc);
            inc = 0;
            for (var j = 0; j < selectedday; j++) {
                inc = inc + 1;
            }
            mindate = selecteddate - inc;
            var start = date.setDate(date.getDate() - inc);
            var startdate = date.getFullYear() + "-" + (date.getMonth() + 1) + "-" + mindate;
            var lastdate = date.getFullYear() + "-" + (date.getMonth() + 1) + "-" + maxdate;
          
            $("#From").val("");
            $("#To").val("");
            $("#From").val(startdate);
            $("#To").val(lastdate);
        });

        function returndayoisition() {

        }
        $(document).on("changeDate", ".noticeboarddate", function (e) {
            var date = $(this).data("datepicker").getDate();
            $(this).val(date);
            //  var day = $(this).data("datepicker").format("dddd");
            formatted = date.getFullYear() + "-" + (date.getMonth() + 1) + "-" + date.getDate();
            // $('#weekDay').val(date.format('dddd'));
            var weekday = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"];
            var selectedday = new Date(date).getDay();
            var selecteddate = new Date(date).getDate();
            $(this).val(formatted);
            //  var selecteddate = new Date(date);
            //var day = weekday[date];      
            //alert(date);
            var mindate = null;
            var maxdate = null;
            var inc = 0;
            for (var i = selectedday; i < 4; i++) {
                inc = inc + 1;
            }
            maxdate = selecteddate + inc;
            var enddate = date.setDate(date.getDate() + inc);
            inc = 0;
            for (var j = 0; j < selectedday; j++) {
                inc = inc + 1;
            }
            mindate = selecteddate - inc;
            var start = date.setDate(date.getDate() - inc);
            var startdate = date.getFullYear() + "-" + (date.getMonth() + 1) + "-" + mindate;
            var lastdate = date.getFullYear() + "-" + (date.getMonth() + 1) + "-" + maxdate;
            $(this).val();
            $("#From").val("");
            $("#To").val("");
            $("#From").val(startdate);
            $("#To").val(lastdate);
        });
        function returndayoisition() {

        }
    </script>

</body>
</html>